function  invertedImage = image_inversion(image)

invertedImage = ~image;
% Detailed explanation is given below
%     negImg = im2double(inputImg);
%     negImg = negImg .* -1;
%     negImg = negImg +1;
%     negImg = im2uint8(negImg);